# Core runtime dependencies (install on the user's machine — full PyPI access)
opencv-python-headless>=4.8
numpy>=1.24
pytesseract>=0.3.10
Pillow>=10.0
deepl>=1.18
requests>=2.28
python-docx>=1.1
PySide6>=6.6

# Optional high-accuracy OCR engine (recommended for best recognition).
# Install these two if you want the "PaddleOCR" engine:
#   pip install paddleocr paddlepaddle
# paddleocr>=2.7
# paddlepaddle>=2.6

# External tools that must be available on PATH (not pip packages):
#   * tesseract-ocr  — https://github.com/tesseract-ocr/tesseract
#       macOS:   brew install tesseract
#       Windows: choco install tesseract   (or the UB-Mannheim installer)
#   * ffmpeg / ffprobe — https://ffmpeg.org
#       macOS:   brew install ffmpeg
#       Windows: choco install ffmpeg
