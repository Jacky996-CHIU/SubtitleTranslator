{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git -c user.name=SubtitleTranslator -c user.email=mra7audi@protonmail.com commit -q -m 'Initial commit: SubtitleTranslator \\(OCR → DeepL → SRT/video/docx\\) *)"
    ]
  }
}
